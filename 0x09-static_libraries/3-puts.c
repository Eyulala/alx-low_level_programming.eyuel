#include "main.h"
void _puts(char *str)
#include<stdio.h>
/**
 * main - Entry point
 * _puts - it prints the word
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
